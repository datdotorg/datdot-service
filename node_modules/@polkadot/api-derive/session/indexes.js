"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.indexes = indexes;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _types = require("@polkadot/types");

var _util = require("../util");

// Copyright 2017-2019 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
function indexes(api) {
  return (0, _util.memo)(() => ( // Some chains (eg. very limited node-template), does not have session
  api.query.session && api.query.staking ? api.queryMulti([api.query.session.currentIndex, api.query.staking.currentEra, api.query.staking.validatorCount]) : (0, _rxjs.of)([(0, _types.createType)(api.registry, 'SessionIndex', 1), (0, _types.createType)(api.registry, 'EraIndex', 1), (0, _types.createType)(api.registry, 'u32')])).pipe((0, _operators.map)((_ref) => {
    let [currentIndex, currentEra, validatorCount] = _ref;
    return {
      currentIndex,
      currentEra,
      validatorCount
    };
  })));
}