"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.referendumInfos = referendumInfos;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _util = require("../util");

var _referendumInfo = require("./referendumInfo");

// Copyright 2017-2020 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
function referendumInfos(api) {
  return (0, _util.memo)((ids = []) => (!(ids === null || ids === void 0 ? void 0 : ids.length) ? (0, _rxjs.of)([]) : api.query.democracy.referendumInfoOf.multi(ids)).pipe((0, _operators.switchMap)(infos => (0, _rxjs.combineLatest)(...ids.map((id, index) => (0, _referendumInfo.retrieveInfo)(api, id, infos[index])))), (0, _operators.map)(infos => infos.filter(referendum => !!referendum))));
}