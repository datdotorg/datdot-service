"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.proposals = proposals;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _util = require("../util");

// Copyright 2017-2019 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
function retrieveProposals(api, hashes) {
  return (0, _rxjs.combineLatest)([api.query.council.proposalOf.multi(hashes), api.query.council.voting.multi(hashes)]).pipe((0, _operators.map)((_ref) => {
    let [proposals, votes] = _ref;
    const result = [];
    proposals.forEach((proposalOpt, index) => {
      if (proposalOpt.isSome) {
        result.push({
          hash: hashes[index],
          proposal: proposalOpt.unwrap(),
          votes: votes[index].unwrapOr(null)
        });
      }
    });
    return result;
  }));
}

function proposals(api) {
  return (0, _util.memo)(() => api.query.council ? api.query.council.proposals().pipe((0, _operators.switchMap)(proposals => retrieveProposals(api, proposals))) : (0, _rxjs.of)([]));
}