"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.validators = validators;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _util = require("../util");

// Copyright 2017-2019 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.

/**
 * @description Retrieve latest list of validators
 */
function validators(api) {
  return (0, _util.memo)(() => ( // Sadly the node-template is (for some obscure reason) not comprehensive, so while the derive works
  // in all actual real-world deployed chains, it does create some confusion for limited template chains
  api.query.session && api.query.staking ? api.queryMulti([api.query.session.validators, api.query.staking.currentElected]) : (0, _rxjs.of)([[], []])).pipe((0, _operators.map)((_ref) => {
    let [validators, currentElected] = _ref;
    return {
      currentElected,
      validators
    };
  })));
}