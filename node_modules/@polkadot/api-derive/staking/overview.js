"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.overview = overview;

var _defineProperty2 = _interopRequireDefault(require("@babel/runtime/helpers/defineProperty"));

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _types = require("@polkadot/types");

var _util = require("../util");

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { (0, _defineProperty2.default)(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function retrievePointsPrev(api, activeEra, currentElected) {
  return api.query.staking.currentEraPointsEarned(activeEra).pipe((0, _operators.map)(({
    individual,
    total
  }) => (0, _types.createType)(api.registry, 'EraRewardPoints', {
    total,
    individual: new Map(individual.map(points => (0, _types.createType)(api.registry, 'RewardPoint', points)).map((points, index) => [currentElected[index], points]))
  })));
}
/**
 * @description Retrieve the staking overview, including elected and points earned
 */


function overview(api) {
  return (0, _util.memo)(() => (0, _rxjs.combineLatest)([api.derive.session.indexes(), api.derive.staking.validators()]).pipe((0, _operators.switchMap)(([indexes, {
    nextElected,
    validators
  }]) => (0, _rxjs.combineLatest)([(0, _rxjs.of)(_objectSpread({}, indexes, {
    nextElected,
    validators
  })), api.query.staking.erasRewardPoints ? api.query.staking.erasRewardPoints(indexes.activeEra) : retrievePointsPrev(api, indexes.activeEra, nextElected)])), (0, _operators.map)(([info, eraPoints]) => _objectSpread({}, info, {
    eraPoints
  }))));
}