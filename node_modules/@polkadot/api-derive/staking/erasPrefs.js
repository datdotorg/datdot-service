"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.eraPrefs = eraPrefs;
exports._erasPrefs = _erasPrefs;
exports.erasPrefs = erasPrefs;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _util = require("../util");

// Copyright 2017-2020 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
function mapPrefs(era, all) {
  const validators = {};
  all.forEach(([key, prefs]) => {
    validators[key.args[1].toString()] = prefs;
  });
  return {
    era,
    validators
  };
}

function eraPrefs(api) {
  return (0, _util.memo)(era => api.query.staking.erasValidatorPrefs.entries(era).pipe((0, _operators.map)(prefs => mapPrefs(era, prefs))));
}

function _erasPrefs(api) {
  return (0, _util.memo)(eras => eras.length ? (0, _rxjs.combineLatest)(eras.map(era => api.derive.staking.eraPrefs(era))) : (0, _rxjs.of)([]));
}

function erasPrefs(api) {
  return (0, _util.memo)(withActive => api.derive.staking.erasHistoric(withActive).pipe((0, _operators.switchMap)(eras => api.derive.staking._erasPrefs(eras))));
}