import { RuntimeVersion } from '@polkadot/types/interfaces';
import { RegistryTypes } from '@polkadot/types/types';
import { Text } from '@polkadot/types';
export declare function getMetadataTypes(version: number): RegistryTypes;
export declare function getChainTypes(chainName: Text, { specName, specVersion }: RuntimeVersion, typesChain?: Record<string, RegistryTypes>, typesSpec?: Record<string, RegistryTypes>): RegistryTypes;
