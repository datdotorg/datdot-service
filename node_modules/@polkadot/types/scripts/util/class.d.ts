import { Constructor } from '../../types';
export declare function isChildClass(Parent: Constructor<any>, Child: Constructor<any>): boolean;
export declare function isCompactEncodable(Child: Constructor<any>): boolean;
