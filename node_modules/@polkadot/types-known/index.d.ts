import { Registry, RegistryTypes, OverrideModuleType } from '@polkadot/types/types';
import BN from 'bn.js';
import { Text } from '@polkadot/types';
/**
 * @description Based on the metadata version, return the registry types
 */
export declare function getMetadataTypes(_registry: Registry, version: number): RegistryTypes;
/**
 * @description Get types for specific modules (metadata override)
 */
export declare function getModuleTypes({ knownTypes }: Registry, section: string): OverrideModuleType;
/**
 * @description Based on the chain and runtimeVersion, get the applicable types (ready for registration)
 */
export declare function getSpecTypes({ knownTypes }: Registry, chainName: Text | string, specName: Text | string, specVersion: BN | number): RegistryTypes;
