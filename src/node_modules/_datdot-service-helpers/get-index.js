module.exports = get_index

async function get_index (feed, index) {
  return new Promise((resolve, reject) => {
    feed.get(index, (err, res) => {
      if (err) reject(err)
      resolve(res)
    })
  })
}