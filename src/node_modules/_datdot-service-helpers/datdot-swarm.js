const hyperswarm = require('hyperswarm')

module.exports = datdot_swarm

async function datdot_swarm (opts) {
  console.log({opts: opts.bootstrap[0]})
  const swarm = new hyperswarm(opts)
  await swarm.flush()
  const swarmAPI = { swarm }

  return swarmAPI
}