const varint = require('varint')

module.exports = parse_decompressed

function parse_decompressed (decompressed, encoder_pos) {
  const offset = varint.encodingLength(encoder_pos)
  const decoded = decompressed.slice(offset)
  return decoded
}