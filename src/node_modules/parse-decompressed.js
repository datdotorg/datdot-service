const varint = require('varint')

module.exports = parse_decompressed

function parse_decompressed (decompressed, encoder_id) {
  const offset = varint.encodingLength(encoder_id)
  const decoded = decompressed.slice(offset)
  return decoded
}