
module.exports = make_swarm

async function make_swarm (data, api) {
  const { store, log } = api
  log({ type: 'make-swarm', data })


  // 3. Make swarm
  swarm = new hyperswarm()
  subcache.swarm = swarm
  swarm.on('connection', onconnection(swarmID, log))


}
