const audit = require('audit-hypercore')

module.exports = merkle_verify

async function merkle_verify (data) {
  return new Promise((resolve, reject) => {
    console.log('merkle verifying')
    const { index, encoded, proof } = data
    return true //TODO
  })
}