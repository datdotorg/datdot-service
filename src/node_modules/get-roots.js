module.exports = get_roots

async function get_roots (feed, index) {
  return new Promise((resolve, reject) => {
    feed.rootHashes(index, (err, res) => {
      if (err) reject(err)
      resolve(res)
    })
  })
}