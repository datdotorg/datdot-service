module.exports = hypercore_close

async function hypercore_close (feed) {
  return new Promise((resolve, reject) => {
    feed.close((err) => {
      if (err) reject(err)
      resolve()
    })
  })
}