module.exports = getChunks

function getChunks (ranges) {
  let count = 0
  const chunks = []
  for (var i = 0, len = ranges.length; i < len; i++) {
    const [min, max] = ranges[i]
    for (var j = min; j < max + 1; j++) {
      chunks.push(j)
      count++
    }
  }
  return { count, chunks }
}
