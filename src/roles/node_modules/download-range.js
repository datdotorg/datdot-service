module.exports = download_range

async function download_range (feed, range) {
  return new Promise((resolve, reject) => {
    feed.download(range, (err) => {
      if (err) {
        console.log({ text: 'error downloading range', err })
        return reject(err)
      }
      resolve()
    })
  })
}